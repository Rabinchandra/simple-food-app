export default class Category  {
    constructor(id, categoryName, uri, items) {
        this.id = id;
        this.categoryName = categoryName;
        this.uri = uri;
        this.items = items;
    }
}